.gradle/
build/
.idea/
*.iml
*.log
local.properties
android/.gradle/
android/build/
android/app/build/

# Local secrets for docker-compose (EIA key, etc.)
.env

# Release signing — keep the keystore and its passwords out of version control
android/keystore.properties
android/release.keystore

# Built APKs (copied to repo root for convenience) — not source
*.apk
